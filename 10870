#include<bits/stdc++.h>
#define MAX 10e12
using namespace std;


int main(){
    int arr[21];
    arr[1] = 1;
    arr[2] = 1;
    for(int i = 3; i <= 20; i++)
        arr[i] = arr[i - 1] + arr[i - 2];
    int N;
    cin >> N;
    cout << arr[N];
}
