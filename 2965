#include <cstdio>
#include <iostream>
#include <queue>
#include <stack>

using namespace std;

int main(){
    int a, b, c;
    cin >> a >> b >> c;
    int tmp;
    tmp =  b - a;
    if(tmp < c - b)
        tmp = c - b;
    cout << tmp - 1 << endl;
}
