#include <string>
#include <vector>
#include <deque>
#include <bits/stdc++.h>
using namespace std;

#define endl "\n"
#define MOD (10e9 + 7)
typedef long long ll;
int N, M, K, L;

int Circle[51][51];

bool check[1001];
int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    cin >> N >> M;
    if(N > 0 && M > 0)
        cout << 1;
    else if(N > 0 && M < 0)
        cout << 4;
    else if(M > 0)
        cout << 2;
    else
        cout << 3;
}

