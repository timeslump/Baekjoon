#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;



int main(){
    vector<int> array;
    int N;
    cin >> N;
    for(int i = 0; i < N; i++){
        int input;
        cin >> input;
        array.push_back(input);
    }
    sort(array.begin(), array.end());
    for(int i = 0; i < N; i++){
        cout << array[i] << endl;
    }
}
