#include<iostream>


using namespace std;


int main(){
    int T;
    cin >> T;
    while(T--){
        int N, M;
        cin >> N >> M;
        if(N < 11 || (N == 11 || M < 4)){
            cout << -1 << endl;
            continue;
        }
        cout << M * 11 + 4 << endl;
    }
}
