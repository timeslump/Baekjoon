#include<bits/stdc++.h>

using namespace std;
#define endl "\n"


int N, Y;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> N;
    cout << N * (N + 1) / 2 << endl;
}
