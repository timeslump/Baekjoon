#include<iostream>
#include<algorithm>
#include <vector>
#include <string>
using namespace std;
int main(){
    int N;
    cin >> N;
    long x, y, z;
    if(N == 2) {
        cin >> x >> y;
        z = 0;
    }
    else {
        cin >> x >> y >> z;
    }
    for(int i = 1; i <= x; i++)
        if(x % i == 0 && y % i == 0 && z % i == 0)
            cout << i << endl;
}
