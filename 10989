#include<iostream>
#include<vector>
#include<queue>
#include<cmath>
using namespace std;

#define ll long long

int Arr[10001] = {0, };
int main(){
    ios_base :: sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int N;
    cin >> N;
    while(N--){
        int tmp;
        cin >> tmp;
        Arr[tmp]++;
    }
    for(int i = 0; i <= 10000; i++){
        if(Arr[i] > 0){
            int tmp = Arr[i];
            while(tmp--) {
                cout << i << "\n";
            }
        }
    }
}
